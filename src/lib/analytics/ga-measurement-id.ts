/**
 * Official GA4 measurement ID for mynanganallur.in.
 * Loaded site-wide via `SiteAnalytics` in `app/layout.tsx` (gtag.js + route tracking).
 */
export const GA_MEASUREMENT_ID = "G-WDPGFYERFD" as const;
